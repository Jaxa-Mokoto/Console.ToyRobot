namespace ToyRobot.Common.Enums
{
    /// <summary>
    /// A container for valid command types.
    /// </summary>
    public enum Commands
    {
        PLACE,
        MOVE,
        LEFT,
        RIGHT,
        REPORT,
        RESTART,
        EXIT
    }
}